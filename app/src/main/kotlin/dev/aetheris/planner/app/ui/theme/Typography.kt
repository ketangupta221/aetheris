/*
 * :app — Typography scaffold for the Aetheris theme (Task 7.1).
 *
 * Phase 0 uses the Material 3 default type scale unchanged. Custom typefaces
 * (e.g. a display-weight serif for ritual screens) land in later polish
 * phases; keeping the defaults here means the theme compiles cleanly and the
 * Timeline_View / OnboardingScreen inherit accessible metrics for free.
 */
package dev.aetheris.planner.app.ui.theme

import androidx.compose.material3.Typography

/** Material 3 default type scale — replace in a future polish phase. */
internal val AetherisTypography: Typography = Typography()
